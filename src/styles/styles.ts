const textFontStyles = {
   fontFamily: 'monospace',
   wordBreak: 'break-word',
   whiteSpace: 'pre-wrap',
   fontWeight: 300,
   fontSize: '12px',
}

const labelFontStyles = {
   fontFamily: 'monospace',
   fontSize: '12px',
   fontWeight: 600,
   lineHeight: 1,
}

export { textFontStyles, labelFontStyles }
